language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-go-angular-starter' ]; then cd .. && eval "mv $currentfolder generator-go-angular-starter" && cd generator-go-angular-starter; fi

